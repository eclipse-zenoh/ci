name: Publish crates (Cargo)

inputs:
  live-run:
    required: false
  branch:
    required: true
  repos:
    required: true
  github-token:
    required: true
  crates-io-token:
    required: true
  inter-deps-pattern:
    required: true

runs:
  using: node20
  main: ../dist/publish-crates-cargo-main.js
